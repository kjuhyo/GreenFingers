import React from "react";
import {
  View,
  Text,
  StyleSheet,
  ImageBackground,
  Image,
  TouchableOpacity,
  ScrollView,
  Dimensions,
} from "react-native";
import { Container, Icon, Button, Content } from "native-base";

export function RoomScreen() {
  // state = {
  //   mainName: "Dasol House",
  // };
  // updateState = () => this.setState({ mainName: "update" });
  return (
    <View style={{ flex: 1 }}>
      <Text>Hi this is room</Text>
    </View>
  );
}
